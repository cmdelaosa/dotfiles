{
  "permissions": {
    "allow": [
      "Bash(./gradlew:*)",
      "Bash(dart:*)",
      "Bash(docker:*)",
      "Bash(echo:*)",
      "Bash(find /Users/cmo/.claude/plugins -type f -name *.json)",
      "Bash(find /Users/cmo/Projects -maxdepth 2 -type f -name *.md)",
      "Bash(flutter:*)",
      "Bash(git:*)",
      "Bash(java:*)",
      "Bash(kotlinc:*)",
      "Bash(make:*)",
      "Bash(nvm --version)",
      "Bash(pnpm:*)",
      "Bash(ruby --version)",
      "Bash(sw_vers)",
      "Bash(xcode-select -p)",
      "Bash(xcrun --version)",
      "Bash(xcrun simctl:*)",
      "WebFetch",
      "WebSearch",
      "mcp__Claude_Browser__find",
      "mcp__Claude_Browser__get_page_text",
      "mcp__Claude_Browser__navigate",
      "mcp__Claude_Browser__preview_list",
      "mcp__Claude_Browser__preview_logs",
      "mcp__Claude_Browser__preview_start",
      "mcp__Claude_Browser__preview_stop",
      "mcp__Claude_Browser__read_console_messages",
      "mcp__Claude_Browser__read_network_requests",
      "mcp__Claude_Browser__read_page",
      "mcp__Claude_Browser__resize_window"
    ],
    "defaultMode": "auto"
  },
  "model": "opus",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "/Users/cmo/.claude/hooks/git-no-main.sh"
          },
          {
            "type": "command",
            "command": "/Users/cmo/.claude/hooks/git-una-sesion-por-checkout.sh"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "/Users/cmo/.claude/hooks/git-una-sesion-por-checkout.sh --aviso"
          },
          {
            "type": "command",
            "command": "/Users/cmo/.claude/hooks/dotfiles-al-dia.sh"
          }
        ]
      }
    ],
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "terminal-notifier -title 'Claude Code' -message 'Needs your input'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "terminal-notifier -title 'Claude Code' -message 'Task finished'"
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "frontend-design@claude-plugins-official": false
  },
  "outputStyle": "concise",
  "effortLevel": "high",
  "autoUpdatesChannel": "latest",
  "skipDangerousModePermissionPrompt": true,
  "skipWorkflowUsageWarning": true,
  "skipAutoPermissionPrompt": true,
  "autoMode": {
    "environment": [
      "### Org-wide",
      "**Organization**: cmdelaosa (personal GitHub account; single-owner projects)",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: PRIVATE — github.com/cmdelaosa/erp is private (confirmed via gh API)",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: None configured — repo carries only `.env.ejemplo` templates (productos/faro/despliegue/.env.ejemplo, productos/norte/despliegue/.env.ejemplo) and `.env` is gitignored",
      "**Default / protected branches**: default branch `main`. Treat `main` as policy-protected wherever it lives: no direct commit/merge/push, changes land through a pull request opened with `gh pr create`. How each repository enforces that is its own business and is not described here: this file is versioned in a public repository.",
      "**CI/CD deploy targets**: GitHub Actions `.github/workflows/verify.yml` runs `pnpm verify` on pull requests and pushes to `main`; deployment is per-client instance (docs/NUEVA-INSTANCIA.md, infra/despliegue/, productos/*/despliegue/) — no automated production deploy target configured from this repo.",
      "**Network posture**: None configured",
      "**Source control**: github.com/cmdelaosa/erp (`origin`, HTTPS) only — as scoped by the Trusted repo and Repository visibility entries above",
      "**Trusted internal domains**: this project's own deployment hosts, as configured under infra/despliegue/ on this machine. The hostnames are deliberately not written here: this file is versioned in a public repository.",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: local Docker Compose stack from infra/docker-compose.yml — Postgres for Norte on port 5433 with API 3001 and Vite 5181, Postgres for Faro on port 5434 with API 3002 and Vite 5182",
      "**Internal package registry**: None configured — public registry.npmjs.org via pnpm (corepack)",
      "**Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar. In this repo that includes the client business records held by the local Postgres services, the data imported by the tools under productos/*/herramientas, the archived documents they generate, and any `.env` file under productos/*/despliegue. Which clients, and which records, is deliberately not written here: this file is versioned in a public repository.",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment (hyphen/underscore/dot-delimited — e.g. matches `prod-db`, not `producer`); plus any per-client deployed instance, which holds real client business data.",
      "**Protected deployment namespaces / environments**: per-client production instances described in docs/NUEVA-INSTANCIA.md and the `infra` namespace seen in session usage; local dev Compose services (norte, faro) are not protected",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment; also applied migrations under packages/db/migraciones (an applied migration is never edited, only superseded) and infra/despliegue/ deployment templates",
      "### User-specific",
      "**Primary use of Claude Code**: software development on this pnpm/TypeScript monorepo (Postgres + Drizzle, NestJS APIs, React/Vite SPAs), with a branch → PR → CI workflow and a `code-reviewer` subagent pass before finishing",
      "**Trusted repo**: /Users/cmo/Projects/erp and its configured remote github.com/cmdelaosa/erp (private). Confidential material is fine in this private repo; secrets and sensitive data are never cleared into any repo by its visibility.",
      "**Org-specific CLIs**: pnpm (via corepack) with repo scripts `pnpm verify`, `pnpm build`, `pnpm test`, `pnpm tests:contar`, `pnpm dev`; `gh` for pull requests; `docker compose -f infra/docker-compose.yml` for the local Postgres stack",
      "**Routine under this repo's prefix**: work happens on a named feature branch and its worktree inside /Users/cmo/Projects/erp; post-merge cleanup (`git pull --ff-only`, `git push origin --delete <branch>`, `git branch -D <branch>`, `git worktree remove <path>`) runs as separate single commands"
    ],
    "allow": [
      "$defaults",
      "Bash(pnpm verify:*) and Bash(pnpm build:*), Bash(pnpm test:*), Bash(pnpm tests:contar:*) in /Users/cmo/Projects/erp — the repo's mandatory local gate",
      "Bash(docker compose -f infra/docker-compose.yml up -d:*) and the matching `down`/`ps`/`logs` for the local norte and faro dev services in /Users/cmo/Projects/erp",
      "Bash(lsof -i:*) for inspecting the local dev ports 3001, 3002, 5181, 5182, 5433, 5434",
      "Bash(gh pr create:*) and Bash(gh pr view:*) against github.com/cmdelaosa/erp"
    ],
    "soft_deny": [
      "$defaults",
      "Bash(docker compose -f infra/docker-compose.yml down -v:*) — removing the local Postgres volumes destroys the Norte/Faro dev databases",
      "Any write or edit to already-applied SQL files under packages/db/migraciones/** in /Users/cmo/Projects/erp — a migration that has been applied is superseded, never modified",
      "Any command setting CLAUDE_ALLOW_MAIN=1 or CLAUDE_ALLOW_SHARED_CHECKOUT=1 — these hook escape hatches are the owner's to authorise, not the agent's"
    ]
  }
}
